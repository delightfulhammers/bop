name: 'AI Code Reviewer'
description: 'AI-powered code review using multiple LLM providers (Anthropic, OpenAI, Gemini)'
author: 'bkyoung'
branding:
  icon: 'code'
  color: 'purple'

inputs:
  # Provider API Keys (at least one required)
  anthropic-api-key:
    description: 'Anthropic API key'
    required: false
  openai-api-key:
    description: 'OpenAI API key'
    required: false
  gemini-api-key:
    description: 'Google Gemini API key'
    required: false

  # Review behavior
  base-branch:
    description: 'Base branch to compare against'
    required: false
    default: 'main'
  reviewers:
    description: 'Comma-separated list of reviewers to use (e.g., security,architecture)'
    required: false
  block-threshold:
    description: 'Severity threshold for blocking (critical, high, medium, low, none)'
    required: false
    default: 'none'

  # Output options
  post-review:
    description: 'Post review comments to PR'
    required: false
    default: 'true'
  fail-on-findings:
    description: 'Fail the action if findings exist above block-threshold'
    required: false
    default: 'false'

  # Advanced
  config-file:
    description: 'Path to cr.yaml config file (auto-detects .github/cr.yaml or cr.yaml if not specified)'
    required: false
  log-level:
    description: 'Log level (trace, debug, info, error)'
    required: false
    default: 'info'

outputs:
  findings-count:
    description: 'Total number of findings'
    value: ${{ steps.review.outputs.findings-count }}
  critical-count:
    description: 'Number of critical severity findings'
    value: ${{ steps.review.outputs.critical-count }}
  high-count:
    description: 'Number of high severity findings'
    value: ${{ steps.review.outputs.high-count }}
  medium-count:
    description: 'Number of medium severity findings'
    value: ${{ steps.review.outputs.medium-count }}
  low-count:
    description: 'Number of low severity findings'
    value: ${{ steps.review.outputs.low-count }}
  summary:
    description: 'Markdown summary of the review'
    value: ${{ steps.review.outputs.summary }}

runs:
  using: 'composite'
  steps:
    - name: Validate inputs
      shell: bash
      env:
        INPUT_ANTHROPIC_KEY: ${{ inputs.anthropic-api-key }}
        INPUT_OPENAI_KEY: ${{ inputs.openai-api-key }}
        INPUT_GEMINI_KEY: ${{ inputs.gemini-api-key }}
        INPUT_POST_REVIEW: ${{ inputs.post-review }}
        EVENT_NAME: ${{ github.event_name }}
      run: |
        set -euo pipefail

        # Check at least one API key is provided
        if [ -z "$INPUT_ANTHROPIC_KEY" ] && [ -z "$INPUT_OPENAI_KEY" ] && [ -z "$INPUT_GEMINI_KEY" ]; then
          echo "::error::At least one API key must be provided (anthropic-api-key, openai-api-key, or gemini-api-key)"
          exit 1
        fi

        # Warn if post-review is enabled on non-PR events
        if [ "$INPUT_POST_REVIEW" = "true" ] && [ "$EVENT_NAME" != "pull_request" ]; then
          echo "::warning::post-review is enabled but this is not a pull_request event. PR comments will not be posted."
        fi

    - name: Install code-reviewer
      shell: bash
      env:
        ACTION_REF: ${{ github.action_ref }}
        GH_TOKEN: ${{ github.token }}
      run: |
        set -euo pipefail

        # Check jq availability (required for JSON parsing)
        if ! command -v jq &> /dev/null; then
          echo "::error::jq is required but not installed. Please ensure jq is available on your runner."
          exit 1
        fi

        # Determine version from action ref
        VERSION="$ACTION_REF"
        # Handle: empty, main branch, or commit SHA (40 hex chars)
        if [ -z "$VERSION" ] || [ "$VERSION" = "main" ] || [[ "$VERSION" =~ ^[0-9a-f]{40}$ ]]; then
          VERSION="latest"
        fi

        echo "Installing code-reviewer ${VERSION}..."

        # Detect OS and architecture with validation
        OS=$(uname -s | tr '[:upper:]' '[:lower:]')
        ARCH=$(uname -m)
        case "$ARCH" in
          x86_64) ARCH="amd64" ;;
          aarch64|arm64) ARCH="arm64" ;;
          *)
            echo "::error::Unsupported architecture: $ARCH. Supported: x86_64, aarch64, arm64"
            exit 1
            ;;
        esac

        case "$OS" in
          linux|darwin) ;;
          *)
            echo "::error::Unsupported OS: $OS. Supported: linux, darwin"
            exit 1
            ;;
        esac

        # Set up install directory (avoid /usr/local/bin for portability)
        INSTALL_DIR="${RUNNER_TEMP}/cr-bin"
        mkdir -p "$INSTALL_DIR"

        # Resolve version and build download URL
        if [ "$VERSION" = "latest" ]; then
          # Query API - use auth if token available (may be empty on fork PRs)
          CURL_OPTS=(-fsSL)
          if [ -n "$GH_TOKEN" ]; then
            CURL_OPTS+=(-H "Authorization: Bearer $GH_TOKEN")
          fi
          LATEST_VERSION=$(curl "${CURL_OPTS[@]}" \
            https://api.github.com/repos/bkyoung/code-reviewer/releases/latest | \
            jq -r '.tag_name')
          if [ -z "$LATEST_VERSION" ] || [ "$LATEST_VERSION" = "null" ]; then
            echo "::error::Failed to determine latest version. Consider pinning to a specific version (e.g., @v0.6.2)"
            exit 1
          fi
          VERSION="$LATEST_VERSION"
        fi

        VERSION_NUM="${VERSION#v}"
        ARCHIVE_NAME="code-reviewer_${VERSION_NUM}_${OS}_${ARCH}.tar.gz"
        DOWNLOAD_URL="https://github.com/bkyoung/code-reviewer/releases/download/${VERSION}/${ARCHIVE_NAME}"
        CHECKSUM_URL="https://github.com/bkyoung/code-reviewer/releases/download/${VERSION}/checksums.txt"

        echo "Downloading from: $DOWNLOAD_URL"

        # Download archive and checksums (use RUNNER_TEMP for cleanup)
        TEMP_DIR=$(mktemp -d -p "${RUNNER_TEMP}")
        curl -fsSL --retry 3 --retry-connrefused "$DOWNLOAD_URL" -o "$TEMP_DIR/$ARCHIVE_NAME"
        curl -fsSL --retry 3 --retry-connrefused "$CHECKSUM_URL" -o "$TEMP_DIR/checksums.txt"

        # Verify checksum
        cd "$TEMP_DIR"
        EXPECTED_CHECKSUM=$(grep -F "$ARCHIVE_NAME" checksums.txt | awk '{print $1}' || true)
        if [ -z "$EXPECTED_CHECKSUM" ]; then
          echo "::error::Checksum not found for $ARCHIVE_NAME"
          exit 1
        fi

        if command -v sha256sum &> /dev/null; then
          ACTUAL_CHECKSUM=$(sha256sum "$ARCHIVE_NAME" | awk '{print $1}')
        else
          # macOS uses shasum
          ACTUAL_CHECKSUM=$(shasum -a 256 "$ARCHIVE_NAME" | awk '{print $1}')
        fi

        if [ "$EXPECTED_CHECKSUM" != "$ACTUAL_CHECKSUM" ]; then
          echo "::error::Checksum verification failed!"
          echo "Expected: $EXPECTED_CHECKSUM"
          echo "Actual:   $ACTUAL_CHECKSUM"
          exit 1
        fi

        echo "Checksum verified successfully"

        # Extract binary
        tar xzf "$ARCHIVE_NAME" -C "$INSTALL_DIR" cr
        chmod +x "$INSTALL_DIR/cr"

        # Add to PATH for subsequent steps
        echo "$INSTALL_DIR" >> "$GITHUB_PATH"

        # Verify installation
        "$INSTALL_DIR/cr" version

        # Cleanup
        rm -rf "$TEMP_DIR"

    - name: Run code review
      id: review
      shell: bash
      env:
        ANTHROPIC_API_KEY: ${{ inputs.anthropic-api-key }}
        OPENAI_API_KEY: ${{ inputs.openai-api-key }}
        GEMINI_API_KEY: ${{ inputs.gemini-api-key }}
        GITHUB_TOKEN: ${{ github.token }}
        # Pass all inputs as environment variables to avoid shell injection
        INPUT_BASE_BRANCH: ${{ inputs.base-branch }}
        INPUT_LOG_LEVEL: ${{ inputs.log-level }}
        INPUT_POST_REVIEW: ${{ inputs.post-review }}
        INPUT_REVIEWERS: ${{ inputs.reviewers }}
        INPUT_CONFIG_FILE: ${{ inputs.config-file }}
        INPUT_FAIL_ON_FINDINGS: ${{ inputs.fail-on-findings }}
        INPUT_BLOCK_THRESHOLD: ${{ inputs.block-threshold }}
        # GitHub context
        GH_HEAD_REF: ${{ github.head_ref }}
        GH_REPO_OWNER: ${{ github.repository_owner }}
        GH_REPO_NAME: ${{ github.event.repository.name }}
        GH_PR_NUMBER: ${{ github.event.pull_request.number }}
        GH_COMMIT_SHA: ${{ github.event.pull_request.head.sha }}
        GH_WORKSPACE: ${{ github.workspace }}
        GH_EVENT_NAME: ${{ github.event_name }}
      run: |
        set -euo pipefail

        # Validate PR context for review - only pull_request supported
        # NOTE: pull_request_target is intentionally NOT supported due to security concerns:
        # it runs in base repo context with secrets, which is dangerous with untrusted PR code
        if [ "$GH_EVENT_NAME" != "pull_request" ]; then
          echo "::error::This action only supports pull_request events (got: $GH_EVENT_NAME). pull_request_target is not supported for security reasons."
          exit 1
        fi
        if [ -z "$GH_HEAD_REF" ]; then
          echo "::error::github.head_ref is not available. The PR branch may have been deleted."
          exit 1
        fi

        # Build command using array to avoid injection
        CMD=(cr review branch "$GH_HEAD_REF")
        CMD+=(--base "$INPUT_BASE_BRANCH")
        CMD+=(--log-level "$INPUT_LOG_LEVEL")

        # Output directory
        OUTPUT_DIR="${RUNNER_TEMP}/review-output"
        mkdir -p "$OUTPUT_DIR"
        CMD+=(--output "$OUTPUT_DIR")

        # GitHub PR posting (only on PR events)
        if [ "$INPUT_POST_REVIEW" = "true" ] && [ "$GH_EVENT_NAME" = "pull_request" ]; then
          CMD+=(--post-github-review)
          CMD+=(--github-owner "$GH_REPO_OWNER")
          CMD+=(--github-repo "$GH_REPO_NAME")
          CMD+=(--pr-number "$GH_PR_NUMBER")
          CMD+=(--commit-sha "$GH_COMMIT_SHA")
        fi

        # Optional reviewers
        if [ -n "$INPUT_REVIEWERS" ]; then
          CMD+=(--reviewers "$INPUT_REVIEWERS")
        fi

        # Config file: explicit input > .github/cr.yaml > cr.yaml > defaults
        CONFIG_FILE=""
        if [ -n "$INPUT_CONFIG_FILE" ]; then
          CONFIG_FILE="${GH_WORKSPACE}/${INPUT_CONFIG_FILE}"
          # Validate config file is within workspace (prevent path traversal)
          # Use Python for portability and proper path comparison
          RESOLVED_CONFIG=$(python3 -c "import os,sys; print(os.path.realpath(sys.argv[1]))" "$CONFIG_FILE" 2>/dev/null || echo "")
          # Use Python's commonpath for safe path-segment comparison (prevents /repo vs /repo-malicious)
          IS_WITHIN=$(python3 -c "
import os,sys
workspace = sys.argv[1]
resolved = sys.argv[2]
try:
    common = os.path.commonpath([workspace, resolved])
    print('yes' if common == workspace else 'no')
except ValueError:
    print('no')
" "$GH_WORKSPACE" "$RESOLVED_CONFIG" 2>/dev/null || echo "no")
          if [ -z "$RESOLVED_CONFIG" ] || [ "$IS_WITHIN" != "yes" ]; then
            echo "::error::Config file must be within the workspace directory"
            exit 1
          fi
        elif [ -f "${GH_WORKSPACE}/.github/cr.yaml" ]; then
          CONFIG_FILE="${GH_WORKSPACE}/.github/cr.yaml"
          echo "::notice::Using config file: .github/cr.yaml"
        elif [ -f "${GH_WORKSPACE}/cr.yaml" ]; then
          CONFIG_FILE="${GH_WORKSPACE}/cr.yaml"
          echo "::notice::Using config file: cr.yaml"
        fi

        if [ -n "$CONFIG_FILE" ]; then
          CMD+=(--config "$CONFIG_FILE")
        fi

        echo "::group::Running code review"
        echo "Command: ${CMD[*]}"

        # Run review, capture exit code
        set +e
        "${CMD[@]}"
        CR_EXIT_CODE=$?
        set -e
        echo "::endgroup::"

        # Parse results using jq (available on GitHub runners)
        TOTAL=0 CRITICAL=0 HIGH=0 MEDIUM=0 LOW=0

        JSON_FILE=$(find "$OUTPUT_DIR" -name "*.json" -type f 2>/dev/null | head -1)
        if [ -n "$JSON_FILE" ] && [ -f "$JSON_FILE" ]; then
          # Validate JSON is parseable before extracting values
          if jq empty "$JSON_FILE" 2>/dev/null; then
            TOTAL=$(jq -r '.total_findings // 0' "$JSON_FILE")
            CRITICAL=$(jq -r '.severity_counts.critical // 0' "$JSON_FILE")
            HIGH=$(jq -r '.severity_counts.high // 0' "$JSON_FILE")
            MEDIUM=$(jq -r '.severity_counts.medium // 0' "$JSON_FILE")
            LOW=$(jq -r '.severity_counts.low // 0' "$JSON_FILE")
          else
            echo "::warning::Failed to parse results JSON - counts may be inaccurate"
          fi
        else
          echo "::warning::No JSON output found - review may have failed"
        fi

        echo "findings-count=$TOTAL" >> "$GITHUB_OUTPUT"
        echo "critical-count=$CRITICAL" >> "$GITHUB_OUTPUT"
        echo "high-count=$HIGH" >> "$GITHUB_OUTPUT"
        echo "medium-count=$MEDIUM" >> "$GITHUB_OUTPUT"
        echo "low-count=$LOW" >> "$GITHUB_OUTPUT"

        echo "::notice::Review complete: $TOTAL findings ($CRITICAL critical, $HIGH high, $MEDIUM medium, $LOW low)"

        # Write summary (with safe truncation)
        MD_FILE=$(find "$OUTPUT_DIR" -name "*_merged_*.md" -type f 2>/dev/null | head -1)
        if [ -n "$MD_FILE" ] && [ -f "$MD_FILE" ]; then
          # Truncate to 64000 chars (leaving buffer for GitHub's 65535 limit)
          head -c 64000 "$MD_FILE" >> "$GITHUB_STEP_SUMMARY"
          # Use unique delimiter to prevent injection (unlikely to appear in markdown)
          DELIM="__CR_SUMMARY_END_$(date +%s)__"
          {
            echo "summary<<${DELIM}"
            head -c 64000 "$MD_FILE"
            echo ""
            echo "${DELIM}"
          } >> "$GITHUB_OUTPUT"
        fi

        # Check threshold (case-insensitive)
        SHOULD_FAIL=0
        if [ "$INPUT_FAIL_ON_FINDINGS" = "true" ]; then
          THRESHOLD=$(echo "$INPUT_BLOCK_THRESHOLD" | tr '[:upper:]' '[:lower:]')
          case "$THRESHOLD" in
            critical)
              (( CRITICAL > 0 )) && SHOULD_FAIL=1 ;;
            high)
              (( CRITICAL > 0 || HIGH > 0 )) && SHOULD_FAIL=1 ;;
            medium)
              (( CRITICAL > 0 || HIGH > 0 || MEDIUM > 0 )) && SHOULD_FAIL=1 ;;
            low)
              (( TOTAL > 0 )) && SHOULD_FAIL=1 ;;
            none)
              ;; # No blocking
            *)
              echo "::error::Invalid block-threshold: $INPUT_BLOCK_THRESHOLD (must be: critical, high, medium, low, none)"
              exit 1
              ;;
          esac

          if [ "$SHOULD_FAIL" -eq 1 ]; then
            echo "::error::Findings exceed block threshold ($INPUT_BLOCK_THRESHOLD)"
            exit 1
          fi
        fi

        # Propagate cr exit code
        if [ "$CR_EXIT_CODE" -ne 0 ]; then
          echo "::error::Code review failed with exit code $CR_EXIT_CODE"
          exit "$CR_EXIT_CODE"
        fi
